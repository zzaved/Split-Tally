# Generated
/build
/.docusaurus
/node_modules

# `markdown/` is generated by `npm run build`, but it is committed on purpose:
# the point of it is that somebody reading the repository finds the whole
# documentation without having to build anything.
